<%= simple_form_for(@mer_warehouse) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :branche_id %>
    <%= f.input :name %>
    <%= f.input :location %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
