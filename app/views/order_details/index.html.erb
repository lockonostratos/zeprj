<h1>Listing order_details</h1>

<table>
  <thead>
    <tr>
      <th>Order</th>
      <th>Product</th>
      <th>Quality</th>
      <th>Return quality</th>
      <th>Price</th>
      <th>Discount cash</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @order_details.each do |order_detail| %>
      <tr>
        <td><%= order_detail.order_id %></td>
        <td><%= order_detail.product_id %></td>
        <td><%= order_detail.quality %></td>
        <td><%= order_detail.return_quality %></td>
        <td><%= order_detail.price %></td>
        <td><%= order_detail.discount_cash %></td>
        <td><%= link_to 'Show', order_detail %></td>
        <td><%= link_to 'Edit', edit_order_detail_path(order_detail) %></td>
        <td><%= link_to 'Destroy', order_detail, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Order detail', new_order_detail_path %>
